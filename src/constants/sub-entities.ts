export enum Common {
  Id = 'id',
  MongoId = '_id',
  IsDeleted = 'isDeleted',
}

export enum TimeStamps {
  CreatedAt = 'createdAt',
  UpdatedAt = 'updatedAt',
}

export enum MissingPosterStatus {
  Missing = 'missing',
  Found = 'found',
  Seen = 'seen',
}
